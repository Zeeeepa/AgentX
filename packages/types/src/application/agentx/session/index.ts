/**
 * Session module - Session management
 *
 * - SessionManager: TypeScript API (agentx.sessions.*)
 *
 * Note: Session operations use WebSocket events, not HTTP endpoints.
 */

export type { SessionManager } from "./SessionManager";
