/**
 * Agent module - Agent lifecycle management
 *
 * - AgentManager: TypeScript API (agentx.agents.*)
 *
 * Note: Agent operations use WebSocket events, not HTTP endpoints.
 */

export type { AgentManager } from "./AgentManager";
