# Test Environment Configuration
#
# This file configures how tests run:
# - If AGENT_API_KEY and AGENT_BASE_URL are set, tests use real Claude API
# - If these are empty or missing, tests use MockProvider (default)
#
# To use real Claude API for testing:
# 1. Copy this file to .env.test
# 2. Fill in your credentials
# 3. Run: pnpm test

# Agent API Configuration
# If both are set, tests will use real Claude Provider
AGENT_API_KEY=
AGENT_BASE_URL=


# Test Configuration
# TEST_MODE is auto-detected based on credentials above
# You can manually override with: TEST_MODE=integration pnpm test
