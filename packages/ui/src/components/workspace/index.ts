/**
 * Workspace - Integration layer for multi-agent chat UI
 *
 * Workspace is the frontend-specific concept that integrates:
 * - useSession (maps to agentx.sessions)
 * - useAgent (maps to agentx.agents)
 * - ContainerView (pure UI layout)
 */

export { Workspace, type WorkspaceProps } from "./Workspace";
